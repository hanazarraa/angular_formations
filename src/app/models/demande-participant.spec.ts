import { DemandeParticipant } from './demande-participant';

describe('DemandeParticipant', () => {
  it('should create an instance', () => {
    expect(new DemandeParticipant()).toBeTruthy();
  });
});
